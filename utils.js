// Helper function to perform some task
function myHelperFunction(arg) {
    return Number(
        value.toString().match(/^\d+(?:\.\d{0,2})?/)
    )
  }
  
// Export the helper function
export { myHelperFunction };